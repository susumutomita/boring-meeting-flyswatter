### ハエ叩きゲーム付き会議退屈度メーター - 2026-04-23

目的:
- ブラウザ上で動く「会議の退屈度メーター + ハエ叩きゲーム」の体験版を実装し、ピッチの核となる価値を 1 画面で伝えられる状態にする。

制約:
- このリポジトリにはまだアプリ本体がないため、`packages/frontend` を新規に立ち上げる。
- `/feature` スキル前提の運用が書かれているが、このセッションでは対話専用ツールが使えないため、ユーザー提供の要件を仕様の起点として実装する。
- モック API は使わず、ローカルだけで完結するフロントエンド体験に限定する。

タスク:
- [completed] 仕様メモと実装計画を追加する。
- [completed] Vite + React のフロントエンドをスキャフォールドする。
- [completed] 会議退屈度ロジックとハエ叩きゲーム UI を実装する。
- [completed] 退屈度計測ロジックのテストを追加する。
- [completed] 依存関係をインストールし、lint / typecheck / test / build を通す。

検証手順:
- `bun install`
- `bun run lint`
- `bun run typecheck`
- `bun run test`
- `bun run build`

進捗ログ:
- 2026-04-23 20:xx JST: リポジトリの現状と `CLAUDE.md` / `.claude` 配下のローカルスキルを確認した。
- 2026-04-23 20:xx JST: `packages/frontend` に Vite + React アプリを追加し、会議監視 UI、退屈度メーター、ハエ叩き介入、メトリクス表示を実装した。
- 2026-04-23 20:xx JST: `meeting.ts` に純粋関数ベースの状態遷移を実装し、`bun test` で 5 件のロジック検証を通した。
- 2026-04-23 20:xx JST: `bun run lint` / `bun run typecheck` / `bun run test` / `bun run build` を全て通過させた。

振り返り:
- 最小構成でも、退屈検知とゲーム介入の関係が一目で分かるデモにはなった。
- 次に広げるなら、実入力検知ではなくても「会議テンポの良し悪し」を模擬するプリセットシナリオを足すと、ピッチで見せやすくなる。

### マリオペイント寄せのハエ叩き調整 - 2026-05-03

目的:
- 添付されたマリオペイントのハエ叩き画面を参照し、ハエを小さく気持ち悪くないドット絵風にして、叩き動作を手カーソル付きの網が上下する挙動へ寄せる。

制約:
- `/feature` スキル前提の運用が書かれているが、このセッションでは利用可能スキル一覧に存在しないため、既存の `Plan.md` 運用と TDD を代替手順にする。
- 添付画像は方向性の参照にとどめ、外部アセットやコピー画像は使わず CSS で再現する。
- ハエは見た目を小さくしつつ、命中範囲は網の広さとして確保する。

タスク:
- [completed] 現状のハエサイズ、CSS 表現、スワッター動作を確認する。
- [completed] 生成されるハエサイズを小さくし、命中範囲を調整する。
- [completed] ハエをドット絵風にし、アリーナ背景を白いグリッドへ寄せる。
- [completed] スワッターを手カーソル付きの網にし、回転ではなく上下に叩く動作へ変更する。
- [completed] lint / typecheck / test / build とブラウザ表示を確認する。

検証手順:
- `bun run lint`
- `bun run typecheck`
- `bun run test`
- `bun run build`
- `agent-browser` で `http://localhost:5173/` を確認する。

進捗ログ:
- 2026-05-03 JST: 添付画像を参照し、リアル寄りのハエ表現と回転スイングを調整対象として確認した。
- 2026-05-03 JST: `createFly` のサイズを 14-20px に下げ、`selectFlyInSwatReach` の標準命中範囲を広げた。
- 2026-05-03 JST: CSS で小さなピクセルハエ、白いグリッド背景、手カーソル付き網スワッターを実装した。
- 2026-05-03 JST: `bun run lint` / `bun run typecheck` / `bun run test` / `bun run build` を通過させ、`agent-browser` でゲーム発火後と叩き動作を確認した。

振り返り:
- 盤面内の説明文を消してスコア中心にすると、白いグリッドと小さなハエが主役になり、マリオペイントに近い軽さが出る。

### ハエの抽象化と撃ち落とし動作 - 2026-05-03

目的:
- ハエがまだ気持ち悪く見える問題を解消し、命中時に標的が下へ撃ち落とされる動きを追加する。

制約:
- 既存のマリオペイント寄せの白いグリッドと手カーソル付き網は維持する。
- 虫の身体や赤い目として読める表現を避け、小さなピクセル記号として扱う。
- 命中判定とスコア処理は既存ロジックを維持し、UI 側で落下演出を挟む。

タスク:
- [completed] 標的の赤目、脚、胴体に見える CSS 表現を削る。
- [completed] 命中した標的を通常表示から外し、落下用のピクセル標的を表示する。
- [completed] 落下後にスコア処理と置き換えを行う。
- [completed] lint / typecheck / test / build とブラウザ表示を確認する。

検証手順:
- `bun run lint`
- `bun run typecheck`
- `bun run test`
- `bun run build`
- `agent-browser` で `http://localhost:5173/` を確認する。

進捗ログ:
- 2026-05-03 JST: 標的を虫の身体ではなく、淡いピクセル記号に変更した。
- 2026-05-03 JST: `knockdowns` 表示状態を追加し、命中した標的が下へ回転しながら落ちる演出を追加した。

振り返り:
- 虫らしい赤目や脚の情報を消し、淡いピクセル記号として扱うほうが、叩く対象として軽く見える。
- 撃ち落とし演出を UI 側で挟むことで、スコア処理だけでは出ない手応えを足せる。

### プロダクトに語らせる UI 簡素化 - 2026-05-03

目的:
- 説明文で価値を語る構成をやめ、ハエ叩き盤面、退屈メーター、最小限の数値だけで体験が伝わる画面にする。

制約:
- `/feature` スキル前提の運用が書かれているが、このセッションでは利用可能スキル一覧に存在しないため、既存の `Plan.md` 運用と品質ゲートで代替する。
- コピー量を増やさず、操作と反応で見せる。
- 既存の会議退屈度ロジックとハエ叩きロジックは維持する。

タスク:
- [completed] ヒーロー、説明、長い状態文を画面から外す。
- [completed] 盤面、スコア、時間、残機、退屈メーターに表示要素を絞る。
- [completed] 下端の HUD がポインター操作を奪わないようにする。
- [completed] lint / typecheck / test / build とブラウザ表示を確認する。

検証手順:
- `bun run lint`
- `bun run typecheck`
- `bun run test`
- `bun run build`
- `agent-browser` で `http://localhost:5173/` を確認する。

進捗ログ:
- 2026-05-03 JST: 長いヒーロー文、説明ノート、詳細な状態説明を削除した。
- 2026-05-03 JST: ゲーム台に近い 1 画面構成へ寄せ、盤面と短いステータスだけを残した。
- 2026-05-03 JST: `bun run lint` / `bun run typecheck` / `bun run test` / `bun run build` を通過させた。
- 2026-05-03 JST: `agent-browser` で下端までスワッターが追従し、命中直後に撃ち落とし表示が出ることを確認した。

振り返り:
- コピーを削るほど、退屈ゲージが溜まってハエ叩きに切り替わる挙動が見えやすくなる。
- 盤面上の HUD は `pointer-events: none` にして、ゲーム操作の邪魔をしない前提を守る必要がある。

### スワッター追従のなめらか化 - 2026-05-03

目的:
- ハエたたきがカーソルに対してカクカクして追従しない感触を解消し、振り下ろしも滑らかなアニメに揃える。

制約:
- 既存の手付き網のビジュアルと、スイング時間 (`swatRecoverDelayMs` 190ms) は維持する。
- React の状態モデルは大きく変えず、DOM 構造と CSS の調整に閉じ込める。

タスク:
- [completed] スワッター位置追従とスイングを別要素に分離する。
- [completed] スイングアニメから `steps(4, end)` を外し、滑らかなイージングに置き換える。
- [completed] `transform` の transition を短くして、カーソル追従の遅延を減らす。
- [completed] lint / typecheck / test / build を通す。

検証手順:
- `bun run lint`
- `bun run typecheck`
- `bun run test`
- `bun run build`

進捗ログ:
- 2026-05-03 JST: カクカクの主因が `key={swingKey}` による外側要素の再マウント、`steps(4, end)` の離散アニメ、`transform 90ms ease-out` の遅延であることを特定した。
- 2026-05-03 JST: 外側 `.fly-swatter` を位置と傾きだけに、内側 `.fly-swatter-anim` をスイング専用にして、再マウントが追従を切らないようにした。
- 2026-05-03 JST: スイングを `cubic-bezier` の滑らかなイージングに変え、`transform` の transition を 60ms linear に詰めた。

振り返り:
- 位置追従とアニメを同じ要素に乗せると、アニメ再起動のたびに transition がリセットされて視覚的にカクつく。役割を別要素に分けるのが要点。
- `steps()` は意図的にコマ送りに見せたいときだけ使う。スイングのような連続的な動きには不向き。
